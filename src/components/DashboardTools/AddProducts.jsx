import React from 'react';

const AddProducts = ({ addProducts }) => {
  // addProducts would have a click/mousedown listener for adding a products card to the
  // current viewable dashboard
  return (
    <li>Products+</li>
  );
};

export default AddProducts;
